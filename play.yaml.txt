---
- hosts: redhat
  become: true
  tasks:
   - name: create a new directory
     file:
       path: /playlab
       state: directory
   - name: create a new file
     file:
       path: /playlab/hosts
       state: touch
